import {
  createForm,
  forwardRefs,
  makeFormProps
} from "./chunk-KOJXZLUD.js";
import "./chunk-VDTIIAEH.js";
import {
  genericComponent,
  makeComponentProps,
  propsFactory,
  useRender
} from "./chunk-CJI4VPK2.js";
import {
  createBaseVNode,
  normalizeClass,
  normalizeStyle,
  ref
} from "./chunk-RFX2DXY4.js";

// node_modules/.pnpm/vuetify@4.0.0_typescript@5._f2fdccde9252f1c27250d15c4f1f3d0c/node_modules/vuetify/lib/components/VForm/VForm.js
var makeVFormProps = propsFactory({
  ...makeComponentProps(),
  ...makeFormProps()
}, "VForm");
var VForm = genericComponent()({
  name: "VForm",
  props: makeVFormProps(),
  emits: {
    "update:modelValue": (val) => true,
    submit: (e) => true
  },
  setup(props, {
    slots,
    emit
  }) {
    const form = createForm(props);
    const formRef = ref();
    function onReset(e) {
      e.preventDefault();
      form.reset();
    }
    function onSubmit(_e) {
      const e = _e;
      const ready = form.validate();
      e.then = ready.then.bind(ready);
      e.catch = ready.catch.bind(ready);
      e.finally = ready.finally.bind(ready);
      emit("submit", e);
      if (!e.defaultPrevented) {
        ready.then(({
          valid
        }) => {
          if (valid) {
            formRef.value?.submit();
          }
        });
      }
      e.preventDefault();
    }
    useRender(() => createBaseVNode("form", {
      "ref": formRef,
      "class": normalizeClass(["v-form", props.class]),
      "style": normalizeStyle(props.style),
      "novalidate": true,
      "onReset": onReset,
      "onSubmit": onSubmit
    }, [slots.default?.({
      errors: form.errors.value,
      isDisabled: form.isDisabled.value,
      isReadonly: form.isReadonly.value,
      isValidating: form.isValidating.value,
      isValid: form.isValid.value,
      items: form.items.value,
      validate: form.validate,
      reset: form.reset,
      resetValidation: form.resetValidation
    })]));
    return forwardRefs(form, formRef);
  }
});
export {
  VForm
};
//# sourceMappingURL=vuetify_components_VForm.js.map
